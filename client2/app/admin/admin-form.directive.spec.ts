import { AdminFormDirective } from './admin-form.directive';

describe('AdminFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminFormDirective();
    expect(directive).toBeTruthy();
  });
});
